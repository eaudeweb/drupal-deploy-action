name: 'Deploy a release to the server'
description: 'Copy and configure a new release to the target server'
inputs:
  ssh_user:
    description: 'SSH user account'
    required: true
  ssh_host:
    description: 'SSH host server'
    required: true
  ssh_key:
    description: 'SSH private key'
    required: true
  artifacts_dir:
    description: 'Absolute path where the artifacts will be deployed'
    required: true
  project_dir:
    description: 'Absolute path where the project root is configured (where live symlink exists)'
    required: true
  settings_file:
    description: 'Absolute path where settings.local.php file is located'
    required: true
  env_file:
    description: 'Absolute path where .env file is located'
    required: false
    default: ''
  robo_file:
    description: 'Absolute path where robo.yml file is located'
    required: false
    default: ''
  public_files_dir:
    description: 'Absolute path where public files directory is located'
    required: true
  private_files_dir:
    description: 'Absolute path where private files directory is located'
    required: false
    default: ''
  artifacts_lifespan:
    description: 'The number of days after which the artifacts are deleted.'
    required: false
    default: '30'
  check_config:
    description: 'Check if there are configuration changes in the Drupal database not exported in config. Fail if true.'
    required: false
    default: 'false'
  sql_backup:
    description: 'Create a SQL database backup before doing the deployment (with release filename)'
    required: false
    default: 'false'

runs:
  using: "composite"
  steps:
    - name: 'Configure SSH'
      run: |
        mkdir -p ~/.ssh/
        echo "$SSH_KEY" > ~/.ssh/server.key
        chmod 600 ~/.ssh/server.key
        cat > ~/.ssh/config <<END
        Host server
          HostName $SSH_HOST
          User $SSH_USER
          IdentityFile ~/.ssh/server.key
          StrictHostKeyChecking no
        END
      env:
        SSH_USER: ${{ inputs.ssh_user }}
        SSH_HOST: ${{ inputs.ssh_host }}
        SSH_KEY: ${{ inputs.ssh_key }}
      shell: bash

    - name: 'Check for Drupal schema changes'
      if:  ${{ inputs.check_config == 'true' }}
      run: ssh server 'cd ${{env.PROJECT_DIR}} && CONFIG_STATUS=$(./vendor/bin/drush config:status -n 2>&1) && echo ${CONFIG_STATUS} | grep -i -q -s "No differences"'
      shell: bash

    - name: 'Create SQL database backup'
      if:  ${{ inputs.sql_backup == 'true' }}
      run: ssh server 'cd ${{env.PROJECT_DIR}} && ./vendor/bin/drush sql:dump --structure-tables-list=cache,cache_*,watchdog,sessions,history --result-file=${{ inputs.artifacts_dir }}/database-before-${{ steps.artifact.outputs.base }}.sql --gzip'
      shell: bash

    - name: 'Deploy release to the server'
      run: |
        ssh server 'rm -rf ${{env.BUILD_DIR}} && mkdir -p ${{env.BUILD_DIR}}'
        scp ${{ env.RELEASE_FILENAME }} server:${{env.BUILD_DIR}}/
        rm ${{ env.RELEASE_FILENAME }}
        ssh server 'cd ${{ env.BUILD_DIR }} && tar zxf ${{ env.RELEASE_FILENAME }} && rm ${{ env.RELEASE_FILENAME }}'
        ssh server 'rm -f ${{ env.PROJECT_DIR }} && ln -s ${{ env.BUILD_DIR }} ${{ env.PROJECT_DIR }}'
        ssh server 'if [ "${{ inputs.settings_file }}" != "" ]; then ln -s ${{ inputs.settings_file }} ${{env.PROJECT_DIR}}/web/sites/default/settings.local.php; fi'
        ssh server 'if [ "${{ inputs.env_file }}" != "" ]; then ln -s ${{ inputs.env_file }} ${{env.PROJECT_DIR}}/.env; fi'
        ssh server 'if [ "${{ inputs.robo_file }}" != "" ]; then ln -s ${{ inputs.robo_file }} ${{env.PROJECT_DIR}}/robo.yml; fi'
        ssh server 'if [ "${{ inputs.public_files_dir }}" != "" ]; then ln -s ${{ inputs.public_files_dir }} ${{env.PROJECT_DIR}}/web/sites/default/files; fi'
        ssh server 'if [ "${{ inputs.private_files_dir }}" != "" ]; then ln -s ${{ inputs.private_files_dir }} ${{env.PROJECT_DIR}}/private; fi'
      env:
        BUILD_DIR: "${{ inputs.artifacts_dir }}/${{ steps.artifact.outputs.base }}"
        RELEASE_FILENAME: "${{ steps.artifact.outputs.filename }}"
        PROJECT_DIR: "${{ inputs.project_dir }}"
      shell: bash

    - name: 'Clean old artifacts'
      run:  ssh server 'cd ${{ inputs.artifacts_dir }} && find release-* -maxdepth 1 -type d -mtime +${{ inputs.artifacts_lifespan }} -prune -exec rm -rf {} \;'
      shell: bash